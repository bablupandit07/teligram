services:
  - type: web
    name: flow-telegram
    runtime: node
    plan: free
    buildCommand: npm install --omit=dev
    startCommand: npm start
    healthCheckPath: /healthz
    envVars:
      - key: HOST
        value: 0.0.0.0
      - key: APP_DATA_DIR
        value: /tmp/flow-telegram
